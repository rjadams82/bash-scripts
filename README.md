# bash-scripts
collection of bash scripts and tools

## new host setup
standard config
`apt install -y nano && apt install -y snmpd && apt install -y bash-autocomplete`

## lazydocker
install lazy docker with one line

`curl -L https://raw.githubusercontent.com/rjadams82/bash-scripts/main/install-lazydocker.sh | bash`

## bash autocomplete
??
`sudo apt install bash-autocomplete`
